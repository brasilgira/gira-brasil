// ============================================================================
// Este arquivo não é mais carregado pelo noticia.html.
//
// A lógica de comentários e curtidas (buscar da API, enviar comentário,
// curtir/descurtir) foi movida pra dentro do próprio componente React em
// noticia.html, porque era ali que o estado da tela (a lista de
// comentários exibida na tela) realmente vivia. Este arquivo antigo
// chamava `window.renderizarComentarios`, uma função que nunca chegou a
// existir no React, então os comentários buscados aqui nunca apareciam
// de fato na tela.
//
// Mantido apenas como referência histórica. Pode ser removido do projeto.
// ============================================================================
